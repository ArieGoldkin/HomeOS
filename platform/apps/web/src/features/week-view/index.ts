// Public barrel for the week-view feature slice. The one responsive Calendar screen;
// Pip / DayRow / WeekList / DayColumn / WeekGrid are internal atoms — not exported.
export { CalendarScreen, type CalendarScreenProps } from "./CalendarScreen";
